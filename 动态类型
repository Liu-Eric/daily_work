1.1 缺少类型声明语句
一个变量在第一次赋值时创建，之后的赋值会改变它，Python在运行代码时会监测变量名，必须在引用前赋值。
变量名没有类型约束，类型的概念存在对象中，变量是通用的，它只是某个环境下绑定到某个对象上而已。
当变量出现在表达式中时，会立刻被它引用的对象替换。
变量到对象的引用通过内存指针实现。
变量是一个系统元素表，拥有指向对象的连接空间。
对象是分配的一块内存，用以保留信息。
每一个对象都有两个头部信息，类型和引用次数，一旦引用次数归零，对象将会被回收。

1.2 共享引用和原处修改
a = 3, b = a #共享引用，a,b应用相同对象。
a = 5 #变量a应用一个新的对象
针对可变对象的共享引用，一个变量对对象的修改会影响另外一个变量。

