1. 传递参数
参数的传递是通过自动将对象赋值给函数本地变量实现的，所有的参数实际上都是通过指针传递的，作为参数被赋值的对象不是自动拷贝。
在函数内部的参数名的赋值不会影响调用者。
改变参数的可变对象参数会对调用者产生影响。

2. 特定的参数匹配类型
默认情况下，参数是通过位置匹配的。
关键字参数，调用通过函数参数名称赋值。
默认参数：定义过程。
可变参数：函数调用中收集任意个数达到基于位置或者关键字的参数。
keyword-only参数：参数必须通过函数名赋值（python3.0）。
func(*sequence)/func(**dict):函数解包
def func(*sequence)/func(**dict):收集匹配多个位置或关键字参数
def func(*[a],name):keyword-only参数
函数调用过程中，先位置参数，后关键字参数，后*sequence，后keyword-only参数， 后**dict
函数定义中，先位置参数，后关键字参数，后*name,后keyword-only参数，后**name

3. 通用set函数

4. 模拟python3.0print的参数
